# Harness 完全体 — PPT 生成文档

> 本文档用于生成 PPT，每节对应 1-N 张幻灯片，包含内容、图表描述和讲解要点。
> 标注 [图表] 的地方需要生成对应图形，标注 [要点] 的地方是讲解者应强调的关键点。

---

## 幻灯片 0：引言 — AI Coding 的演化

### 幻灯片 0-1：三次范式跃迁

**标题：** 从 Prompt 到 Harness：AI 编程的三个时代

**内容：**

三列并排展示：

第一列 — Prompt Engineering（2023-2024）：
- 核心问题：跟 AI 说什么？
- 关键技术：零样本/少样本提示、思维链（CoT）、角色扮演、提示链
- 瓶颈：模型够强后边际效益降低，"说清楚"不再是核心问题

第二列 — Context Engineering（2024-2025）：
- 核心问题：AI 该知道什么？
- 关键技术：RAG 检索增强、上下文压缩、滚动摘要、单一事实来源
- 瓶颈：信息够了但行为仍不可控，Agent 会顺手重构不该动的模块

第三列 — Harness Engineering（2025-2026）：
- 核心问题：系统如何可靠运转？
- 关键机制：约束（不越界）+ 规格（不跑偏）+ 循环（可收敛）+ 驾驭（可信任）
- 本文档的 Harness 架构即此阶段的工程化落地

[图表] 三列并排时间线，每列用不同颜色（黄/蓝/绿），列间用箭头连接，箭头上标注跃迁原因（"模型够强了" / "Agent 失控了"）

[要点] 三者是嵌套关系，不是替代关系。Harness 包含 Context 包含 Prompt。

---

### 幻灯片 0-2：Vibe Coding 的工程化护栏

**标题：** 氛围编程很爽，但需要护栏

**内容：**

上半部分 — Vibe Coding 的四大痛点：
- 代码正确性失控：AI 生成"看起来正确但实际错误"的幻觉代码
- 架构一致性缺失：快速迭代导致意大利面条式代码
- 需求歧义与翻译损耗：自然语言缺乏精确性，AI 自由发挥空间过大
- 缺乏可追溯性：没有单一事实来源，团队协作困难

下半部分 — 五大方法论作为护栏（分层金字塔）：

| 层级 | 方法论 | 定位 | 解决的问题 |
|------|--------|------|-----------|
| 战略层 | SDD（规格驱动开发） | 做什么 | 规格作为唯一事实来源 |
| 架构层 | DDD（领域驱动设计） | 怎么组织 | 统一语言、限界上下文 |
| 验收层 | ATDD（验收测试驱动） | 行为是否正确 | 纯领域语言规格，防止实现细节泄露 |
| 执行层 | TDD（测试驱动开发） | 如何正确实现 | Red-Green-Refactor 循环 |
| 协作层 | BDD（行为驱动开发） | 业务如何描述 | 跨角色统一语言 |

[图表] 上半部分：4 个红色痛点卡片横排；下半部分：5 层金字塔，每层标注方法论名称、定位和对应架构映射

[要点] 这五大方法论的组合，让 Vibe Coding 从"靠感觉"进化为"有护栏、可追溯、可重复"的工程化范式。每种方法论在本架构中都有对应实现。

---

### 幻灯片 0-3：控制面逐层收紧 + 架构映射

**标题：** Rule → Spec → Loop → Harness：从约束到信任

**内容：**

上半部分 — 四步渐进路径（横向流程图）：

1. **Rule（不要乱来）** → 让 AI 先学会不越界
   - 先写 NEVER 和 DO NOT，约束高代价错误
   - 对应实现：CLAUDE.md + Hooks 安全保护层

2. **Spec（这次只做什么）** → 把模糊意图收敛为可执行变更
   - 回答 5 件事：做什么/不做什么/允许改什么/不能动什么/怎样算完成
   - 对应实现：L1 OpenSpec 4 工件强制顺序

3. **Loop（怎么持续收敛）** → 把一次性生成改造为闭环执行
   - 每轮改动足够小→外置状态→检查失败就停下来修
   - 对应实现：L2 Superpowers TDD 循环 + L7 HyperSpec 状态机

4. **Harness（凭什么被信任）** → AI 产出纳入验证、评审、放行体系
   - Contract 守住共享边界 + Hooks 前移检查 + CI 裁决放行
   - 对应实现：L3 双轨审查 + 安全保护层

下半部分 — Harness 衰变定律：
- 模型能力越强，所需 Harness 越简单
- 精力集中在：模型短期内无法解决的问题 + 即使模型再强也无法自行建立的外部接口
- 能动态调整 Harness "厚度"的团队，工程效率回报最高

[图表] 上半部分：4 个卡片横向排列，每个卡片标注演化阶段、核心问题、架构映射；下半部分：衰变曲线简图（模型能力 X 轴 vs Harness 复杂度 Y 轴，反比曲线）

[要点] 四步是严格递进的依赖关系。没有前一步，后一步就失去意义。这解释了为什么我们的架构有 7 层，每一层都在收紧控制面。

---

## 幻灯片 1：封面

**标题：** Harness — AI 辅助开发的工程化工作流架构

**副标题：** 从"随意对话"到"工程化流水线"

**内容：**
- 版本：完全体
- 定位：制造业系统 AI 辅助开发工作流
- 核心理念：规范先行 | 纪律内置 | 审查自适应 | 编排自动化

[要点] 开场强调：这不是一个 AI 工具介绍，而是一套完整的工程化架构，把 AI 从"聊天助手"变成"可控的开发流水线"。

---

## 幻灯片 2：我们要解决什么问题

**标题：** 没有 Harness 时，AI 辅助开发是什么样的？

**内容：**

左侧（问题）：
- 需求理解偏差——AI 凭猜测写代码，写完发现方向错了
- 没有测试——AI 写的代码没有测试保障，改一个地方坏另一个地方
- 审查缺失——代码质量完全依赖人工审查，AI 生成的"看起来正确"但"实际有坑"
- 知识断层——每个会话都是全新的，上次踩过的坑下次还会踩
- 过度审查或审查不足——要么所有改动都跑完整审查流程浪费时间，要么干脆不审查

右侧（Harness 的回答）：
- 规范先行：先定义清楚做什么（proposal/specs/design/tasks），再写代码
- TDD 强制：新代码必须有测试，红→绿→重构
- 自适应审查：改一个字段 3 分钟审查，跨模块重构走完整 9 关
- 越用越聪明：自动积累项目知识，隐性约定不再靠口口相传

[图表] 左右对比布局，左侧红色问题，右侧绿色解决方案，用箭头一一对应

[要点] 核心痛点是"不可控"——AI 写代码很快，但方向、质量、安全性都不可控。Harness 就是让 AI 开发变得可控。

---

## 幻灯片 3：核心设计理念

**标题：** 7 个设计理念

**内容：**

1. **规范先行** — 需求不清不写代码。先 WHAT（做什么），后 HOW（怎么做）
   - 实现方式：OpenSpec 4 工件强制顺序（proposal → specs → design → tasks）

2. **纪律内置** — TDD 不是可选项，是新代码的强制约束
   - 实现方式：Superpowers always_active 技能，AI 自动遵循红绿重构

3. **审查自适应** — 改一个字段不跑 9 关审查，跨模块重构不省略任何关卡
   - 实现方式：双轨审查 + 自动路由（quick-review 3 关 vs gstack 9 关）

4. **编排自动化** — 一条命令启动全自动流程，断了能恢复
   - 实现方式：HyperSpec 12 checkpoint 状态机 + 断点恢复

5. **领域感知** — AI 知道你们项目的 Spring Boot 分层、JPA 规范、平台包用法
   - 实现方式：ECC 领域技能库（springboot/jpa/api 等 10+ 技能）

6. **工具精确** — 代码分析基于 LSP/AST 结构，不靠文本猜测
   - 实现方式：OMC MCP 工具服务器（18+ 工具）

7. **越用越聪明** — 跨会话积累项目知识，隐性约定自动沉淀
   - 实现方式：ECC 本能系统（观察→捕获→应用→分享）

[要点] 7 个理念不是空谈，每个都有对应的工具和实现机制。这是架构的灵魂，不是装饰。

---

## 幻灯片 4：7 层架构总览

**标题：** Harness 完全体 — 7 层架构

[图表] 生成分层架构图，从上到下：

```
┌───────────────────────────────────────────────────────────┐
│                    用户交互层                               │
│       手动路径 (/opsx:*)     │    自动路径 (/hyperspec)    │
└──────────────┬──────────────────────────────┬─────────────┘
               │                              │
┌──────────────▼──────────────────────────────▼─────────────┐
│               L7 编排层 — HyperSpec                         │
│   12 checkpoint │ 断点恢复 │ 审查路由 │ 通知回调             │
├────────┬──────────┬────────────┬──────────────────────────┤
│ L1     │ L2       │ L3         │      L4-L6 增强层         │
│ 规范层  │ 纪律层    │ 审查层      │                          │
│        │          │            │ ┌──────────────────────┐  │
│OpenSpec│Super-    │轻量3关     │ │ L6 持续学习层         │  │
│        │powers    │(quick-     │ │   ECC 本能系统        │  │
│proposal│          │review)     │ ├──────────────────────┤  │
│specs   │ TDD      │            │ │ L5 工具精度层         │  │
│design  │ 子代理    │完整9关     │ │   OMC MCP LSP/AST    │  │
│tasks   │ 验证     │(gstack)    │ ├──────────────────────┤  │
│        │          │            │ │ L4 领域知识层         │  │
│        │          │            │ │   ECC 技能库          │  │
│        │          │            │ └──────────────────────┘  │
├────────┴──────────┴────────────┴──────────────────────────┤
│               安全保护层 — Hooks                            │
├───────────────────────────────────────────────────────────┤
│               模型路由层 — haiku/sonnet/opus                 │
└───────────────────────────────────────────────────────────┘
```

**颜色建议：**
- L1 规范层：蓝色系
- L2 纪律层：绿色系
- L3 审查层：橙色系（轻量和完整用深浅区分）
- L4-L6 增强层：紫色系
- L7 编排层：红色系（顶层编排）
- 安全保护层：灰色
- 模型路由层：青色

**各层功能说明：**
- L1 规范层（OpenSpec）：管理需求工件（proposal/specs/design/tasks）的创建和流转
- L2 纪律层（Superpowers）：TDD 红绿重构强制约束、子代理开发、系统化调试
- L3 审查层（quick-review / gstack）：双轨审查，轻量 3 关和完整 9 关自动路由
- L4 领域知识层（ECC 技能库）：项目领域编码模式（Spring Boot、JPA、API 等）
- L5 工具精度层（OMC MCP）：LSP/AST 级代码分析，提升审查和实现精度
- L6 持续学习层（ECC 本能系统）：跨会话积累项目知识，自动沉淀隐性约定
- L7 编排层（HyperSpec）：12 checkpoint 状态机，全流程自动化编排与断点恢复
- 安全保护层（Hooks）：自动拦截危险操作，保护关键文件和配置
- 模型路由层：按任务复杂度选择 haiku/sonnet/opus，优化成本

[要点] 这是整个架构的核心图。强调两点：1）分层设计，每层职责单一，互不干扰；2）各层叠加式协作，整体能力远大于单层之和。

---

## 幻灯片 5-9：AI 工作流程 — AI 如何沿着 Harness 架构工作

**标题（幻灯片 20）：** AI 会话的完整生命周期

**内容：**

AI 不是"从零开始"的。打开项目后，AI 沿着 Harness 各层经历 6 个阶段：

```
①会话启动 → ②需求理解 → ③计划拆解 → ④编码实施 → ⑤审查归档 → ⑥学习沉淀
```

[图表] 横向 6 阶段流程图，每个阶段用圆形或卡片表示，箭头从左到右串联，下方标注各阶段激活的 Harness 层

[要点] 核心观点：AI 不是随意写代码的，它在每一层都有约束和辅助。

---

**标题（幻灯片 21）：** 阶段 ① — 会话启动：AI 如何"认识"你的项目

**内容：**

AI 进入项目后，第一件事不是写代码，而是加载上下文、建立认知。

```
开发者打开 Claude Code
    │
    ├→ Hook SessionStart 触发
    ├→ 读取 AGENTS.md → 找到 Harness 入口
    ├→ 读取 CLAUDE.md → 加载编码规则
    ├→ using-superpowers 元技能 → 扫描 56+ 技能 + 18+ 工具
    ├→ ECC 本能系统 → 加载历史项目知识
    └→ HyperSpec 状态检测 → 是否有未完成的 checkpoint？
```

**AI 此时的"大脑"里有什么：**
- 编码规范（Entity 禁用 Lombok、Service 继承 BaseServiceManager...）
- 所有可用技能（springboot/jpa/api 等领域知识）
- 所有可用工具（LSP 悬浮/定义跳转、AST 搜索/替换）
- 历史项目知识（"上次改这个字段出了问题"）
- 当前工作状态（有/无未完成的变更）

[图表] 同心圆或思维导图，中心是 "AI 上下文"，外圈是 5 个加载来源：AGENTS.md、CLAUDE.md、技能清单、本能知识、HyperSpec 状态

[要点] AI 不是"失忆"的。每次进入项目，它会自动加载所有上下文，像一个已经入职的团队成员。

---

**标题（幻灯片 22）：** 阶段 ② — 需求理解：AI 如何"想清楚"做什么

**内容：**

开发者发出 `/opsx:propose` 后，AI 进入需求理解阶段：

1. **项目感知**（模型路由 → haiku 快速扫描）
   - 自动探测：Spring Boot + MyBatis + Vue
   - 生成 project_profile

2. **需求对话**（模型路由 → opus 深度分析）
   - 一次只问一个问题，结合历史本能提问
   - 示例："上次改这个字段时出了问题，这次要注意什么？"

3. **生成工件**（L1 规范层 — OpenSpec）
   - 严格按顺序：proposal → specs → design → tasks
   - tasks 粒度自动拆到 2-5 分钟
   - 每个任务注入 TDD 5 步强约束

4. **TDD 计划**（L2 纪律层）
   - 为每个 task 生成"写失败测试→确认失败→写实现→确认通过→提交"计划

5. **规范审查**（L3 审查层 — 复杂度路由）
   - 中大需求 → gstack CEO/Eng 视角审查
   - 小改动 → 自动跳过

6. **通知回调**
   - "需求工件已生成，请审核 proposal"

[图表] 垂直流程图，6 步从上到下，每步右侧标注使用的 Harness 层和模型

[要点] 需求阶段 AI 用的是 opus（最强模型），因为"方向比速度重要"。

---

**标题（幻灯片 23）：** 阶段 ④ — 编码实施：AI 如何"按规矩"写代码

**内容：**

这是 Harness 最核心的部分 — AI 不是随意写代码，而是在 6 层约束下工作。

**对每个 Task，AI 严格遵循 TDD 循环：**

| 步骤 | AI 做什么 | 同时激活的层 |
|------|-----------|-------------|
| 1. 写失败测试 | 先写测试，不写生产代码 | L2 纪律（TDD） |
| 2. 确认失败 | 跑测试，Hook 自动编译检查 | Hooks（run_checks） |
| 3. 写最小实现 | 按领域规范写代码 | L4 领域（springboot/jpa）+ L5 工具（LSP/AST）+ L6 学习（本能参考） |
| 4. 确认通过 | 跑测试，Hook 自动编译 | Hooks（run_checks） |
| 5. 提交 | 自动 commit，更新 checkpoint | L7 编排（HyperSpec） |

**AI 在写每一行代码时的内心独白：**
- L2 纪律层："我不能直接写代码，必须先写失败的测试"
- L4 领域层："我知道 Entity 禁用 Lombok，Service 要继承 BaseServiceManager"
- L5 工具层："我看到了这个类的实际依赖关系，不是猜的"
- L6 学习层："上次踩过坑，这个字段改了要同步改另一个表"
- 安全保护层："我不能写 application.yml，不能没有 change 就改代码"

[图表] TDD 循环图（红→绿→蓝），每个步骤周围环绕标注激活的 Harness 层

[要点] 6 层同时工作，AI 就像一个被"培训过"的团队成员，知道规矩、知道技术栈、知道历史教训。

---

**标题（幻灯片 19）：** 阶段 ⑤⑥ — 审查归档与学习沉淀

**内容：**

**审查归档：**
- L3 审查层自动路由：简单变更走 3 关，复杂变更走 9 关
- L5 工具层辅助审查：AST 搜索 SQL 拼接，LSP 检查编译警告
- 审查失败 → AI 自动修复 → 重新审查
- 归档 → 移入 openspec/archive/，合并 specs

**学习沉淀（AI 如何"记住"经验）：**
- Hook SessionEnd 触发知识捕获
- 分析本次代码变更，识别隐性知识
- 示例沉淀：
  - "修改 OrderService 时必须同步更新 Inventory"
  - "这个项目的统一响应格式是 Result<T>"
  - "DTO 命名规范是 XxxDTO，不分 Param/Query/VO"

**知识积累效果：**

| 使用次数 | AI 积累的知识 | 效果 |
|----------|-------------|------|
| 第 1 次 | 仅 CLAUDE.md 规则 | 像新人入职 |
| 第 5 次 | 10+ 条隐性约定 | 像入职一周 |
| 第 20 次 | 50+ 条项目知识 | 像团队老人 |

[图表] 左侧审查流程（3关/9关决策树），右侧学习曲线（使用次数 vs 知识量，逐步上升的曲线）

[要点] 两个核心价值：1）审查不是"再看一遍"，是按清单系统化检查；2）AI 会越来越聪明，因为每次经验都会沉淀。

---

## 幻灯片 10-11：L1 规范层 — OpenSpec

**标题（幻灯片 20）：** 规范先行：先 WHAT 后 HOW

**内容：**

核心理念：需求不清不写代码。将模糊的需求转化为结构化的工程工件。

4 工件创建顺序（强制，不可跳步）：

```
proposal ──────→ specs ──────→ design ──────→ tasks
  WHY            WHAT           HOW            执行清单
  为什么做        做什么          怎么做          逐条执行
```

| 工件 | 回答的问题 | 内容 | 谁审 |
|------|-----------|------|------|
| proposal | WHY - 为什么做 | 变更动机、边界、做什么不做什么 | 人类必审 |
| specs | WHAT - 做什么 | 需求规格、接口定义、数据结构 | 人类选审 |
| design | HOW - 怎么做 | 技术方案、涉及模块、表名、接口 | 人类必审 |
| tasks | 执行 | 2-5 分钟粒度的原子任务清单 | 人类审粒度 |

[图表] 水平流程图，4 个节点从左到右，每个节点标注 WHY/WHAT/HOW/执行，下方列出每个工件的内容

**标题（幻灯片 21）：** OpenSpec 命令体系

**内容：**

| 命令 | 作用 | 何时用 |
|------|------|--------|
| `/opsx:explore` | 反问澄清需求（不生成工件） | 需求不清晰时 |
| `/opsx:propose` | 生成 4 工件 | 需求明确后 |
| `/opsx:continue` | 单步推进下一个工件 | 想逐步审核时 |
| `/opsx:ff` | 一次生成全部工件 | 简单需求快速推进 |
| `/opsx:verify` | 验证代码与规范对齐 | 实施完成后 |
| `/opsx:apply` | 按 tasks 逐条执行 | 准备写代码时 |
| `/opsx:archive` | 归档变更 | 流程结束时 |

[要点] 强调"先 specs 后 design"——先明确做什么（需求），再设计怎么做（技术方案）。这是避免"方向错了"的关键。

---

## 幻灯片 17-18：L2 纪律层 — Superpowers

**标题（幻灯片 22）：** 纪律内置：TDD 是强制约束

**内容：**

核心理念：没有测试的代码不是完成的代码。

**TDD 红绿重构（always_active，AI 自动遵循）：**

```
🔴 Red          🟢 Green          🔵 Refactor
写失败测试  →    写最小实现   →    在测试保护下重构
确认失败        确认通过          确认仍然通过
```

**每个 task 的 5 步强制流程：**

1. 写失败测试（附完整测试代码）
2. 跑测试——确认失败（附命令+预期输出）
3. 写最小实现（附完整实现代码）
4. 跑测试——确认通过（附命令+预期输出）
5. 提交（附 git 命令）

[图表] 红绿灯视觉设计，红→绿→蓝三色圆圈横向排列，每个圆圈下有说明文字

**标题（幻灯片 23）：** Superpowers 技能矩阵

**内容：**

| 技能 | 触发方式 | 作用 |
|------|----------|------|
| test-driven-development | always_active | TDD 红绿重构（自动生效） |
| verification-before-completion | always_active | 完成前必须验证（自动生效） |
| subagent-driven-development | 按需 `/superpowers:subagent-driven-development` | 并行子代理开发 |
| writing-plans | 按需 `/superpowers:writing-plans` | 拆细任务为 TDD 计划 |
| systematic-debugging | 按需 `/superpowers:systematic-debugging` | 系统化调试 |
| brainstorming | 创意工作前自动触发 | 需求探索 |
| code-review | 完成任务后自动触发 | 代码审查 |

**关键区别：**
- always_active 技能：不需要手动调用，AI 写代码时自动遵循
- 按需技能：需要显式调用或被其他技能触发

[要点] TDD 不是"建议"，是"约束"。新代码必须有测试，这是 Harness 的铁律。

---

## 幻灯片 19-21：L3 审查层 — 双轨审查

**标题（幻灯片 19）：** 审查自适应：不杀鸡用牛刀

**内容：**

核心问题：所有改动都跑完整 9 关审查，对日常 CRUD/修 Bug 来说太重了。
- 改一个字段跑 9 关，命中率为零的关卡浪费 3-5 分钟
- CEO/Design 视角对制造业 CRUD 毫无意义
- 每关一次 AI 调用，串行 9 次累计等待时间长

解决方案：双轨审查，按复杂度自动路由。

[图表] 左右对比：左侧"gstack 9 关"用深橙色大块，右侧"quick-review 3 关"用浅橙色小块，中间用问号标注"自动选择"

**标题（幻灯片 20）：** 轻量审查 — quick-review

**内容：**

适用：日常 CRUD 字段变更、Bug 修复、小接口调整

触发条件（全部满足）：task ≤ 3 ∧ 单模块 ∧ 无 DDL ∧ 无跨服务

3 关流水线：

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  第 1 关      │     │  第 2 关      │     │  第 3 关      │
│ spec-align    │────→│ risk-check    │────→│ archive      │
│ 规范对齐      │     │ 风险检查      │     │ 归档         │
│               │     │              │     │              │
│ ·代码对齐tasks│     │ ·SQL安全     │     │ (复用现有)   │
│ ·Spring分层   │     │ ·空值处理    │     │              │
│ ·DTO/Entity   │     │ ·索引感知    │     │              │
│ ·平台包       │     │ ·隐性约定    │     │              │
│ ·通用质量     │     │ ·接口兼容    │     │              │
└──────────────┘     └──────────────┘     └──────────────┘
   1 次 AI 调用         1 次 AI 调用         复用 /opsx:archive
```

效果：3 次 AI 调用 vs gstack 9 次，省约 60-70% 等待时间

[图表] 3 个横向排列的卡片，每个卡片列出检查项

**标题（幻灯片 21）：** 自动路由 — 智能选择审查强度

**内容：**

```
                    ┌──────────────────┐
                    │   进入审查阶段    │
                    └────────┬─────────┘
                             │
                    ┌────────▼─────────┐
                    │   分析变更特征    │
                    │ ·task 数量        │
                    │ ·涉及模块数       │
                    │ ·是否有 DDL       │
                    │ ·是否跨服务       │
                    └────────┬─────────┘
                             │
               ┌─────────────▼─────────────────┐
               │ task ≤ 3                       │
               │ AND 模块 ≤ 1                   │
               │ AND 无 DDL                     │
               │ AND 无跨服务                    │
               └──────┬──────────────┬──────────┘
                      │ 是            │ 否
               ┌──────▼──────┐ ┌─────▼──────────┐
               │quick-review │ │ gstack          │
               │  轻量 3 关   │ │ 完整 9 关       │
               │  ~3 分钟    │ │ ~10-15 分钟     │
               └─────────────┘ └────────────────┘
```

**用户可覆盖：**
- `/quick-review` — 强制轻量（不管复杂度）
- `/full-review` — 强制完整（不管复杂度）

**真实场景比例：** 制造业日常开发中，约 70-80% 的变更走轻量审查，20-30% 走完整审查。

[图表] 决策树图，顶部一个菱形决策节点，下方两个分支

[要点] 这是审查层最核心的设计。不是去掉 gstack，而是让日常开发不再被 gstack 的重量拖慢。复杂任务依然有完整审查保障。

---

## 幻灯片 12-13：L7 编排层 — HyperSpec

**标题（幻灯片 22）：** 一条命令，全自动编排

**内容：**

HyperSpec 是纯编排层，不重写任何原生 skill 的功能，只负责：

1. **项目感知** — 自动探测语言/框架/构建工具，生成 project_profile
2. **状态检测** — 读写 .hyperspec-state.yaml，确定当前阶段
3. **阶段路由** — 自动调用对应的原生 skill
4. **审查路由** — 自动判断轻量或完整审查
5. **通知回调** — 关键节点通知人类
6. **Commit 纪律** — 每个 task 完成后自动 commit

**三阶段流程：**

```
/hyperspec
    │
    ├── Propose 阶段
    │   需求确认 → 生成工件 → TDD 计划 → 规范审查
    │
    ├── Apply 阶段
    │   TDD 开发 → 编译检查 → 自动 commit → 验证 → 审查
    │
    └── Archive 阶段
        一致性验证 → 归档 → 清理状态
```

[图表] 3 个大圆形或卡片横向排列，分别标注 Propose/Apply/Archive，内部列出子步骤

**标题（幻灯片 23）：** 12 Checkpoint 状态机

**内容：**

```
profiler-done ──→ requirements-confirmed ──→ openspec-generated
                                                  │
                    ┌─────────────────────────────┘
                    ▼
plan-generated ──→ plan-generated-and-confirmed ──→ task-1-complete
                                                         │
                    ┌────────────────────────────────────┘
                    ▼
task-2-complete ──→ ... ──→ task-N-complete ──→ verified
                                                   │
                    ┌──────────────────────────────┘
                    ▼
reviewed ──→ apply-done ──→ consistency-verified ──→ archived ──→ done
```

**断点恢复：** 中断后重新运行 `/hyperspec`，读取 checkpoint，验证文件状态，路由到断点继续。
- 原则：实际文件状态是 ground truth，状态文件只是缓存

**通知回调点：**
- 🔔 openspec-generated → "需求已生成，请审核"
- 🔔 plan-generated-and-confirmed → "即将开始编码"
- 🔔 reviewed → "审查已完成"
- 🔔 archived → "流程结束"

[图表] 线性状态流转图，12 个节点横向排列，关键节点用不同颜色标注

[要点] 状态机的核心价值是"断了能恢复"。AI 会话超时、网络中断、下班关机，第二天继续即可。

---

## 幻灯片 14：L4 领域知识层 — ECC 技能库

**标题：** 领域感知：AI 知道你们项目的规矩

**内容：**

Harness 内置了制造业 Spring Boot 项目的领域编码模式：

| 技能 | 提供什么 | 何时激活 |
|------|----------|----------|
| springboot-patterns | 分层架构、Bean 管理、配置模式 | 写 Spring 代码时 |
| springboot-security | 认证授权、权限控制 | 涉及安全代码时 |
| springboot-tdd | Spring Boot 测试模式、Mock 策略 | 写测试时 |
| jpa-patterns | Entity 设计、关联映射、N+1 问题 | 涉及 Entity/Mapper 时 |
| api-design | RESTful API 设计、DTO 设计、版本管理 | 设计/实现接口时 |
| database-migrations | 数据库迁移策略、DDL 安全 | 涉及 DDL 时 |
| backend-patterns | 缓存、异常处理、日志 | 通用后端代码 |
| postgres-patterns | PostgreSQL 特有模式、性能优化 | 涉及 SQL 优化时 |
| deployment-patterns | 部署模式、环境配置 | 涉及部署时 |
| docker-patterns | Docker 最佳实践 | 涉及容器化时 |

**来源：** Everything Claude Code (ECC) 项目提取

**集成方式：** 放入 `.claude/skills/` 目录，HyperSpec apply 阶段根据文件类型自动激活

[要点] 这是让 AI 从"通用程序员"变成"你们团队的程序员"的关键。不是教 AI 写代码，而是教 AI 写符合你们规范的代码。

---

## 幻灯片 15：L5 工具精度层 — OMC MCP

**标题：** 工具精确：LSP/AST 级代码分析

**内容：**

传统审查：AI 靠文本匹配分析代码（正则匹配 import、SQL 字符串）
Harness 审查：AI 通过 LSP/AST 获取精确的代码结构信息

| 工具 | 类型 | 用途 |
|------|------|------|
| lsp_hover | LSP | 查看类型信息 |
| lsp_goto_definition | LSP | 跳转到定义 |
| lsp_find_references | LSP | 查找所有引用 |
| lsp_diagnostics | LSP | 编译错误/警告 |
| ast_grep_search | AST | 按语法结构搜索 |
| ast_grep_replace | AST | 按语法结构替换 |

**审查增强效果对比：**

| 审查场景 | 传统方式 | Harness 方式 |
|----------|----------|---------|
| Spring 分层审查 | 正则匹配 import 语句 | LSP 分析实际类依赖 |
| SQL 风险审查 | 文本搜索 SQL 字符串 | AST 定位 SQL 构建模式 |
| 代码引用分析 | grep 搜索 | LSP find_references 精确定位 |
| 类型安全 | 看注释 | LSP hover 实时类型检查 |

**来源：** Oh-My-Claudecode (OMC) 项目提取

[图表] 对比表格，左侧"传统"用灰色，右侧"Harness"用绿色

[要点] 从"猜"到"知道"。这不是锦上添花，是从根本上提升审查准确率。

---

## 幻灯片 16：L6 持续学习层 — ECC 本能系统

**标题：** 越用越聪明：自动积累项目知识

**内容：**

传统方式：隐性约定靠手写文档 + 口口相传
Harness 方式：AI 自动观察、捕获、应用项目知识

**学习内容示例：**

| 知识类型 | 示例 |
|----------|------|
| 隐性约定 | "这个字段改了必须同步改那个表" |
| 踩坑经验 | "这个 Service 不能直接注入，要用工厂获取" |
| 团队偏好 | "我们团队习惯用 QueryDSL 不用 JPQL" |
| API 变更 | "这个接口已废弃，用新接口" |

**工作机制：**

```
观察（会话开始）  →  捕获（会话结束）  →  应用（开发中）  →  分享（团队间）
   加载已有本能      提取隐性知识         自动参考本能       导入导出共享
```

**来源：** Everything Claude Code (ECC) 本能系统

[要点] 这是真正的"越用越好用"。第 10 次使用 Harness 时，AI 已经知道了你们项目 50+ 条隐性约定，不再犯低级错误。

---

## 幻灯片 17：安全保护层 — Hooks

**标题：** 自动保护：不需要记得，系统帮你守

**内容：**

3 个 Hook 脚本，在 AI 操作文件和执行命令时自动运行：

| 时机 | Hook | 作用 | 示例 |
|------|------|------|------|
| 写文件前 | guard_write.py | 拦截对保护目录的写入 | 阻止 AI 修改 application.yml |
| 执行命令前 | ensure_change_context.py | 无活跃 change 时阻止风险命令 | 没创建 change 就不让改代码 |
| 写文件后 | run_checks.sh | Java 文件保存后自动编译检查 | 改了代码立即编译验证 |

**权限控制：**
- 禁止：git push、kubectl、rm -rf、写 application*.yml、写 sql/、写 secrets/
- 允许：git status/diff、mvn compile、openspec *、Edit、ls、cat

[要点] Hooks 是"隐形保镖"。开发者不需要记住所有规则，系统自动阻止危险操作。

---

## 幻灯片 18：模型路由层

**标题：** 成本优化：按任务选模型

**内容：**

不同任务用不同级别的 Claude 模型，优化成本：

| 级别 | 模型 | 成本 | 适用场景 | Harness 中的应用 |
|------|------|------|----------|-----------------|
| 低 | haiku | $ | 快速查询、简单搜索 | explore、轻量审查 |
| 中 | sonnet | $$ | 标准实现、常规审查 | apply、quick-review |
| 高 | opus | $$$ | 架构决策、深度分析 | propose、gstack 审查 |

**来源：** Oh-My-Claudecode (OMC) 模型路由系统

**估算节省：** 日常开发中约 60% 的操作用 haiku/sonnet 即可，整体成本降低 40-50%

[图表] 3 层金字塔，底层 haiku（宽）、中层 sonnet、顶层 opus（窄），每层标注应用场景

---

## 幻灯片 19-20：完整开发流程 — 手动路径

**标题（幻灯片 19）：** 手动路径：逐命令控制

**内容：**

适合：需要精细控制每一步、小改动快速完成

[图表] 垂直流程图：

```
Phase 1: 创建需求
  /opsx:explore          ← 反问澄清（可选）
  /opsx:propose          ← 生成 4 工件
  人工审核                ← proposal 边界 + tasks 粒度

Phase 2: 规范审查（中大需求）
  gstack /office-hours   ← 6 个灵魂拷问
  gstack /plan-ceo-review ← CEO 视角
  writing-plans           ← 拆细 tasks
  gstack /plan-eng-review ← 工程经理视角

Phase 3: 实施
  /opsx:apply            ← 逐条执行
  [Superpowers TDD 自动生效]
  [ECC 领域技能自动激活]
  [OMC MCP 辅助实现]

Phase 4: 审查（自动路由）
  简单 → quick-review 3 关
  复杂 → gstack 9 关

Phase 5: 提交归档
  git commit + /opsx:archive
```

**标题（幻灯片 20）：** 按复杂度裁剪

**内容：**

| 复杂度 | 推荐步骤 | 跳过 |
|--------|----------|------|
| 小改动（改字段/修 Bug） | propose → 人工审 → apply → quick-review → archive | 跳过规范层审查 |
| 中等需求（新接口/新页面） | propose → 人工审 → office-hours → writing-plans → apply → quick-review → archive | 跳过 CEO/Eng/Design 审查 |
| 大需求（跨模块/新模块） | 完整流程 | 不跳 |

[要点] 手动路径的核心优势是"可裁剪"。不需要每步都走，根据复杂度灵活选择。

---

## 幻灯片 21-22：完整开发流程 — HyperSpec 全自动路径

**标题（幻灯片 21）：** HyperSpec 路径：一条命令全自动

**内容：**

适合：中大需求、希望全自动、断了能恢复

[图表] 3 段垂直流程图：

```
/hyperspec

╔══════════════════════════════════════════════╗
║  Propose 阶段                                ║
║                                              ║
║  项目感知 → 需求确认 → 生成 4 工件            ║
║     → TDD 计划 → [中大需求] 规范层审查        ║
║                                              ║
║  🔔 通知: "需求已生成，请审核"                  ║
╚════════════════════╤═════════════════════════╝
                     │
╔════════════════════▼═════════════════════════╗
║  Apply 阶段                                  ║
║                                              ║
║  自动选择执行模式:                             ║
║   ≥6 task → subagent 并行                     ║
║   ≤5 task → inline 串行                      ║
║                                              ║
║  每个 Task:                                   ║
║   TDD 开发 → 编译检查 → 自动 commit           ║
║                                              ║
║  验证 → 审查（自动路由: quick/gstack）         ║
║                                              ║
║  🔔 通知: "审查已完成"                         ║
╚════════════════════╤═════════════════════════╝
                     │
╔════════════════════▼═════════════════════════╗
║  Archive 阶段                                ║
║                                              ║
║  一致性验证 → 归档 → 清理状态                  ║
║                                              ║
║  🔔 通知: "流程结束"                           ║
║  本能系统自动捕获知识                           ║
╚══════════════════════════════════════════════╝
```

**标题（幻灯片 22）：** 断点恢复

**内容：**

```
    第一次运行                    中断                  第二次运行
    /hyperspec                   (关机/超时)            /hyperspec
        │                            │                      │
        ▼                            │                      ▼
   ┌─────────┐                       │               ┌──────────┐
   │ Propose  │                       │               │ 读取     │
   │ 完成     │─────── ✕ ────────────┘               │ checkpoint│
   └────┬────┘                                           │
        │          中断在 Apply                     ┌────▼─────┐
        ▼          的 task-3                         │ 验证文件  │
   ┌─────────┐                                      │ 状态     │
   │ Apply    │                                      └────┬─────┘
   │ task-1 ✓ │                                           │
   │ task-2 ✓ │                                      ┌────▼─────┐
   │ task-3 ✗ │ ← 断点                                 │ 路由到   │
   └─────────┘                                        │ task-3   │
                                                      └────┬─────┘
                                                           │
                                                           ▼
                                                      继续执行
                                                      task-3 → task-4 → ...
```

[要点] 自动路径的核心价值：1）一条命令搞定；2）断了能恢复，不浪费之前的工作。

---

## 幻灯片 23-24：典型场景演示

**标题（幻灯片 23）：** 场景演示：新增一个字段（轻量审查）

**内容：**

```
步骤 1: /opsx:propose
  AI: "为物料主数据新增'安全库存'字段"
  → 生成 proposal/specs/design/tasks

步骤 2: 人工审核（1 分钟）
  ✓ proposal 边界正确
  ✓ tasks 粒度合理（2 个 task）

步骤 3: /opsx:apply
  Task 1: Entity 加字段 + getter/setter
    → TDD: 先写测试 → 确认失败 → 写实现 → 确认通过
    → ECC jpa-patterns 自动激活
  Task 2: DTO 加字段 + Mapper XML
    → TDD: 先写测试 → 确认失败 → 写实现 → 确认通过

步骤 4: 审查路由
  判断: task=2, 模块=1, 无DDL → quick-review

步骤 5: quick-review 3 关
  第 1 关 spec-align: ✓ 代码对齐、分层合规、DTO 规范
  第 2 关 risk-check: ✓ 字段可空、无 SQL 风险
  第 3 关 archive: ✓ 归档

总耗时: ~5 分钟（完整 9 关审查需 ~15 分钟）
```

[图表] 时间线图，标注每个步骤的耗时

**标题（幻灯片 29）：** 场景演示：新增业务模块（完整审查）

**内容：**

```
步骤 1: /hyperspec          ← 一条命令启动
步骤 2: 回答需求确认问题
步骤 3: AI 自动生成 proposal/specs/design/tasks + TDD 计划
步骤 4: gstack 规范层审查（自动触发，因为是大需求）
步骤 5: 人工审核 + 确认"开始实现"
步骤 6: AI 自动完成
  → subagent 并行开发（12 个 task）
  → 每个 task: TDD → 编译 → commit
  → ECC springboot/jpa 技能自动激活
  → OMC MCP LSP 辅助
步骤 7: gstack 完整 9 关审查（自动路由，因为是复杂任务）
步骤 8: 自动归档 + 通知

总耗时: ~30-60 分钟（大部分是 AI 自动完成，人类只需审核）
```

[要点] 用实际场景让听众感受两种路径的差异。小改动 5 分钟，大需求全自动。

---

## 幻灯片 25：工具集成地图

**标题：** 工具集成：每个工具在架构中的位置

**内容：**

[图表] 矩阵图，展示每个工具/插件/skill 的来源、安装位置、所属层级：

| 工具 | 来源 | 安装位置 | 层级 |
|------|------|----------|------|
| OpenSpec | @fission-ai/openspec | npm 全局 | L1 规范层 |
| Superpowers | github.com/obra/superpowers | ~/.claude/plugins/ | L2 纪律层 |
| gstack | github.com/garrytan/gstack | .claude/skills/ | L3 审查层 |
| quick-review | **自建** | .claude/skills/ | L3 审查层 |
| HyperSpec | github.com/wind7rui/HyperSpec | .claude/skills/ | L7 编排层 |
| ECC springboot-* | ECC 项目提取 | .claude/skills/ | L4 领域层 |
| ECC jpa/api/migration | ECC 项目提取 | .claude/skills/ | L4 领域层 |
| OMC MCP Server | OMC 项目提取 | MCP 服务器 | L5 工具层 |
| ECC 本能系统 | ECC 项目提取 | skill + Hook | L6 学习层 |

[要点] 强调"来源明确"——每个工具都有成熟的开源项目支撑，不是从零自建。自建的只有 quick-review 一个。

---

## 幻灯片 26：两条路径选择指南

**标题：** 什么时候用手动？什么时候用自动？

**内容：**

| 维度 | 手动路径 | HyperSpec 路径 |
|------|----------|----------------|
| 入口 | 逐个 `/opsx:*` 命令 | 一条 `/hyperspec` |
| 谁编排 | 你自己 | HyperSpec 自动 |
| 自动 commit | 无（手动） | 有（每 task 自动） |
| 断点恢复 | 无 | 有 |
| 适合 | 小改动、需要精细控制 | 中大需求、全自动 |
| 审查 | 自动路由 | 自动路由 |

**选择建议：**

| 需求复杂度 | 推荐路径 | 理由 |
|------------|----------|------|
| 小改动（改字段、修 Bug） | 手动 | propose → apply → quick-review → archive，4 步完成 |
| 中等需求（新接口、新页面） | 两条都行 | 手动可跳过不需要的审查关；自动省心 |
| 大需求（跨模块、多 Service） | HyperSpec | 自动编排 + 断点恢复 + 自动 commit |

**铁律：** 同一个 change 要么全程手动，要么全程 HyperSpec。不要混用。

---

## 幻灯片 27：总结

**标题：** Harness — 让 AI 开发可控、可测、可追溯

**内容：**

**核心价值：**
1. **可控** — 规范先行，需求不清不写代码
2. **可测** — TDD 强制，每行代码有测试保障
3. **自适应** — 双轨审查，按复杂度匹配审查强度
4. **自动化** — 一条命令全自动，断了能恢复
5. **领域化** — AI 知道你们项目的规矩
6. **精确化** — LSP/AST 级代码分析
7. **自进化** — 越用越聪明，知识自动积累

**设计原则：** 分层解耦，每层职责单一；叠加协作，整体能力远大于单层之和；灵活可裁剪，按复杂度选择路径。

---

## 幻灯片 28：Q&A

**标题：** 问题与讨论

**预备 Q&A：**

Q: 只想用部分能力，Harness 还能用吗？
A: 能。Harness 是叠加式设计。只装 OpenSpec 就是 proposal→apply→archive 的完整闭环，按需添加更多层即可。不装 ECC/OMC 也不影响基础功能。

Q: 两条路径能混用吗？
A: 不能。同一个 change 要么全程手动，要么全程 HyperSpec。混用会导致状态不一致。

Q: 轻量审查会不会漏掉问题？
A: quick-review 覆盖了日常开发中最常见的风险（SQL、空值、分层、隐性约定），只是省去了对简单改动无意义的关卡（CEO 视角、Design 审查、QA 测试等）。复杂任务依然走完整 9 关。
